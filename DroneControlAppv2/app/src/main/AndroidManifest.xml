<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dronecontrolappv2">

    <!-- Uprawnienie do dostępu do Internetu -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DroneControlAppv2">


        <!-- Główna aktywność Twojej aplikacji -->
        <activity
            android:name="com.example.dronecontrolappv2.MainActivity"
            android:exported="true"
            android:screenOrientation="landscape">
            <!-- "landscape" wymusza tryb poziomy, usuń jeśli niepotrzebne -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>